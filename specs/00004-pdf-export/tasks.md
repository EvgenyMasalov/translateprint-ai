# Tasks: Professional PDF Export

**Project Mode**: Brownfield

## Phase 1: Preparation
- [X] T001 Add `html2pdf.js` CDN script tag to `index.html`, `agent.html`, and `registration.html` (for consistency)
- [X] T002 Create a hidden HTML template for the PDF report in `index.html` (styled with Tailwind)

## Phase 2: Core Logic — Editor Export [US1] 🎯 MVP
- [X] T003 Implement `generatePDF()` helper function in `index.html` to populate template and trigger download
- [X] T004 Replace existing text-only export in `index.html` with the new PDF export logic
- [X] T005 Verify PDF layout for standard songs (handling long lyrics and analysis blocks)

## Phase 3: Agent Pro Integration [US2] 🎯 MVP
- [X] T006 Add the hidden PDF template to `agent.html`
- [X] T007 Implement `generatePDF()` logic in `agent.html`, including the "Refined Version" from agents
- [X] T008 Update the "Export" button in `agent.html` to trigger PDF generation

## Phase 4: Styling & Branding
- [X] T009 Add LyricAI Studio branding (header/logo style) to the PDF template
- [X] T010 Add "Generated by LyricAI Studio" footer and timestamp to each PDF page
- [X] T011 Ensure PDF template uses `!text-slate-900` and `!bg-white` to override dark mode themes

## Phase 5: Polish
- [X] T012 Add a loading state (spinner) to the export button during PDF generation
- [X] T013 Verify that filename includes the song title and date (e.g., "MySong_2026-05-03.pdf")
- [X] T014 Manual QA: Export a song with full analysis from both pages
